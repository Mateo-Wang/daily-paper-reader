---
title: Relationally Grounded Latent World Models for Autonomous Driving
title_zh: 面向自动驾驶的关系锚定潜在世界模型
authors: "Fabian Schmidt, Markus Enzweiler, Abhinav Valada"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24626v1"
tags: ["query:driving"]
score: 9.0
evidence: 以交通场景图为监督的自动驾驶潜世界模型，提升轨迹预测精度
tldr: "潜世界模型可预测自动驾驶场景，但状态中的关系语义往往隐式存在。本文基于LAW，从nuScenes三维标注构建以行为者为中心的场景图，用冻结文本嵌入编码其关系结构，并在训练中将其作为特权语义监督对齐视觉潜表示。推理时移除监督分支，无需场景图或三维标注，也不增加测试计算。在nuScenes上，轨迹平均L2误差从0.661降至0.622（5.9%），碰撞率从0.456降至0.217（52.4%），并优于非结构化描述式语义目标。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24626v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 825, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24626v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24626v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1814, \"height\": 914, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.24626v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24626v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 506, \"label\": \"Table\"}]"
motivation: 潜世界模型对自动驾驶预测重要，但其潜状态中的关系语义常为隐式，缺乏显式结构约束，限制表征质量与安全性。
method: 基于LAW，从nuScenes三维标注构建以行为者为中心的场景图，用冻结文本嵌入编码关系结构，训练时对齐视觉潜表示，推理时移除监督分支。
result: "在nuScenes上，平均轨迹L2误差从0.661降至0.622（5.9%），碰撞率从0.456降至0.217（52.4%）。"
conclusion: 显式关系结构作为特权监督可提升潜世界模型表征，且不增加推理开销，并优于非结构化语义目标。
---

## 摘要
潜在世界模型为自动驾驶学习预测性表征，但这些状态所保留的关系语义往往仍是隐式的。我们研究交通场景图能否作为潜在世界表征的特权语义监督。在 LAW 的基础上，我们从 nuScenes 3D 标注构建以交通参与者为中心的场景图，使用冻结的文本嵌入模型对其序列化的关系结构进行编码，并在训练中将视觉潜在表征与该语义目标对齐。在推理阶段我们移除该监督分支，因此它既不需要场景图也不需要 3D 标注，且不增加测试时的计算量。在 nuScenes 上，相对于我们重新训练的 LAW 基线，我们的方法将平均轨迹 L2 误差从 0.661 降至 0.622（5.9%），并将碰撞率从 0.456 降至 0.217（52.4%）。它还优于非结构化的描述式语义目标，支持了显式关系结构对潜在世界模型表征学习的益处。

## Abstract
Latent world models learn predictive representations for autonomous driving, but the relational semantics these states preserve often remain implicit. We investigate whether traffic scene graphs can serve as privileged semantic supervision for latent world representations. Building on LAW, we construct actor-centric scene graphs from nuScenes 3D annotations, encode their serialized relational structure using a frozen text embedding model, and align the visual latent representations with this semantic target during training. We remove the supervision branch at inference, so it requires neither scene graphs nor 3D annotations and adds no test-time computation. On nuScenes, our method reduces average trajectory L2 error from 0.661 to 0.622 (5.9%) and collision rate from 0.456 to 0.217 (52.4%) relative to our retrained LAW baseline. It also outperforms an unstructured caption-style semantic target, supporting the benefit of explicit relational structure for latent world-model representation learning.

---

## 论文详细总结（自动生成）

# 论文总结：Relationally Grounded Latent World Models for Autonomous Driving

## 1. 核心问题与整体含义

- **研究背景**：端到端自动驾驶正从统一感知—规划架构，发展到具有丰富语义表征的基础模型式智能体；同时，潜在世界模型通过紧凑特征空间预测未来场景，便于与下游规划结合，如 LAW、WoTE、World4Drive 等。
- **核心问题**：现有潜在世界模型虽能通过未来视觉潜变量、几何先验或规划目标学习预测性表征，但其潜状态中“交通参与者之间的关系语义”往往是隐式的，没有被显式约束。
- **研究动机**：在驾驶场景中，一个交通参与者的重要性不仅取决于外观，还取决于其与自车的相对位置、方向和交互关系；显式关系结构可能提升潜世界模型的安全性与交互感知能力。
- **整体含义**：论文探索“交通场景图能否作为特权语义监督”来训练潜在世界模型。监督只在训练阶段使用，推理阶段完全移除，因此不增加测试时计算，也不需要场景图或 3D 标注。

## 2. 方法论

- **总体思想**：基于 LAW 潜在世界模型，在训练时额外引入场景图语义对齐目标，使视觉潜表示保留交通场景中的关系语义；推理时保持原 LAW 流程不变。
- **场景图构建与序列化**：
  - 从 nuScenes 3D 标注构建 actor-centric 场景图。
  - 节点包括自车、车辆、骑行者、行人等交通参与者。
  - 边编码相对方向、邻近性、可见性等关系。
  - 参考 GraphPilot 的 Actor-Only 表示，将图序列化为 subject–relation–object 结构化文本。
- **语义目标编码**：
  - 使用冻结的文本嵌入模型 Qwen3-Embedding 对序列化场景图编码，得到场景图语义嵌入 \(z^{SG}_t\)。
  - 文本编码器不参与梯度更新，提供固定语义目标。
- **视觉潜表示对齐**：
  - 对 LAW 的视觉潜变量 \(V_t\) 做 mean pooling，再经 MLP 对齐头 \(P\) 投影到同一语义空间，得到 \(z^{vis}_t\)。
  - 使用余弦距离作为对齐损失，鼓励视觉潜表示捕获场景图编码的高层关系语义。
- **可选时序语义对齐**：
  - 构造未来时刻 \(t+1\) 的场景图并编码为 \(z^{SG}_{t+1}\)。
  - 将世界模型预测的未来潜变量 \(\hat{V}_{t+1}\) 经同一对齐头投影，得到 \(\hat{z}^{vis}_{t+1}\)。
  - 同样用余弦距离对齐，鼓励预测未来状态保留未来交通场景的关系语义。
- **训练目标与推理**：
  - 总损失为原始 LAW 损失加上当前状态对齐损失和可选时序对齐损失，权重分别为 \(\lambda_{SG}\) 和 \(\lambda_{temp}\)。
  - 所有场景图、文本嵌入、对齐头仅在训练时使用，推理时丢弃，不改变 LAW 推理路径，也不增加推理计算量。

## 3. 实验设计

- **数据集与 benchmark**：
  - 使用 nuScenes 规划 benchmark。
  - 基于 perception-free LAW 架构，LAW 本身直接使用多视图图像；本文训练时额外使用 nuScenes 3D 标注构建场景图监督。
  - 指标为预测自车轨迹的 L2 位移误差和碰撞率，预测时域为 1s、2s、3s 及其平均值，越低越好。
- **对比方法**：
  - 重新训练的 LAW 基线。
  - Unstructured Text：使用相同冻结嵌入模型和对齐目标，但语义目标为 caption-style 非结构化描述。
  - Scene Graph：本文提出的结构化场景图监督。
- **消融实验**：
  - 变化当前状态对齐权重 \(\lambda_{SG}\)：0、0.05、0.10、0.25、0.50、1.00。
  - 变化时序对齐权重 \(\lambda_{temp}\)：0.10、0.50。
  - 联合当前—未来监督：\((\lambda_{SG}, \lambda_{temp})\) 取 (0.05,0.05)、(0.10,0.10)、(0.25,0.25)、(0.50,0.50) 等。
- **控制条件**：
  - 所有变体使用相同 LAW 架构和训练设置，官方超参数与训练 schedule。
  - 主要报告相对于重新训练 LAW 基线的改进，而非已发布结果或 checkpoint。

## 4. 资源与算力

- 论文明确说明：所有实验在 **四块 NVIDIA H200 GPU** 上从零训练 LAW，使用官方超参数和训练 schedule。
- 使用 **冻结的 Qwen3-Embedding-8B** 作为文本嵌入模型。
- 论文未明确报告训练时长、总 GPU 小时数、能耗、额外训练开销、模型参数量或推理延迟等细节。
- 由于监督分支仅在训练时使用，推理阶段不增加计算量，但训练阶段引入文本编码和对齐头的额外成本未被详细量化。

## 5. 实验数量与充分性

- **实验规模**：
  - 主实验比较 3 种设置：LAW、Unstructured Text、Scene Graph。
  - 消融表包含约 12 行配置，覆盖当前状态对齐权重、时序对齐权重和联合权重。
- **充分性**：
  - 消融较系统地分析了 \(\lambda_{SG}\) 和 \(\lambda_{temp}\) 的影响，并比较了结构化与非结构化语义目标，能较好支持“关系结构有价值”的结论。
  - 控制实验较公平：重新训练 LAW 基线，所有变体共享相同架构和训练设置，非结构化文本也使用相同冻结嵌入模型与对齐目标。
- **不足**：
  - 仅在 nuScenes 单一数据集、LAW 单一架构上验证，缺少跨数据集、跨模型泛化实验。
  - 未报告多次随机种子、方差、置信区间或统计显著性检验。
  - 时序扩展仅作为消融，未在更广泛设置中验证。
  - 缺少闭环评估和真实部署场景验证。

## 6. 主要结论与发现

- 结构化场景图监督在 nuScenes 上一致提升所有预测时域表现。
- 相对于重新训练 LAW 基线：
  - 平均 L2 误差从 **0.661 降至 0.622**，相对降低 **5.9%**。
  - 平均碰撞率从 **0.456 降至 0.217**，相对降低 **52.4%**。
- 相对于非结构化 caption-style 语义目标：
  - 平均 L2 误差进一步降低 **7.2%**。
  - 平均碰撞率进一步降低 **30.4%**。
  - 非结构化文本虽降低碰撞率至 0.312，但 L2 略升至 0.670，说明显式关系结构更有效。
- 权重消融：
  - \(\lambda_{SG}=0.1\) 取得最佳整体折中：L2 0.622、碰撞率 0.217。
  - 效果并非随权重单调增加，过大权重不能持续带来收益，说明场景图对齐更适合作为辅助约束。
- 时序对齐：
  - 单独 \(\lambda_{temp}=0.1\) 可降低平均 L2 至 0.628，但不改善碰撞。
  - \(\lambda_{temp}=0.5\) 会明显恶化碰撞
