---
title: "BLInD: Learning Driver Intent as a Distribution over Future Ego Trajectories"
title_zh: BLInD：将驾驶员意图学习为未来自车轨迹的分布
authors: "Flavian Pegado, Ronit Hire, Shreyas Rajesh, Soham Phade"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.13941v1"
tags: ["query:driving"]
score: 8.0
evidence: 将驾驶员意图建模为未来自车轨迹分布
tldr: "现有意图预测多依赖场景上下文，仅用车态历史的盲模型又常退化为单一路径，难以提供多模态未来。BLInD仅以速度、曲率、转向灯和车型等历史，学习未来自车轨迹的top-k分布，并比较自回归与流匹配两种架构。流匹配在Wayve、Waymo和nuScenes上ADE/FDE为0.15/0.37、0.15/0.36、0.28/0.59米，且无需针对特定数据集适配即可跨域泛化。用于AEB触发时，严格全候选策略把误报从1.51%降至0.06%并保持98.7%真正例率，在车载NVIDIA DRIVE Orin上流匹配头仅耗时2.9毫秒，适合实时部署。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.13941v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1284, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.13941v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1444, \"height\": 314, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.13941v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 826, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13941v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 613, \"height\": 170, \"label\": \"Table\"}]"
motivation: 现有学习式分布模型依赖场景上下文，而仅用车态历史的盲模型常退化为单一路径，缺少可跨域部署的多模态意图分布。
method: BLInD仅输入速度、曲率、转向灯和车型等车态历史，分别以自回归和流匹配建模未来自车轨迹的top-k分布，并在混合平台开源数据与Wayve数据上训练。
result: "流匹配在Wayve、Waymo、nuScenes上ADE/FDE为0.15/0.37、0.15/0.36、0.28/0.59米，无需数据集适配即可跨域泛化；AEB误报降至0.06%，Orin上耗时2.9毫秒。"
conclusion: BLInD同时实现学习式、盲输入与跨域泛化，为下游系统提供可控且合理的意图采样接口，并以AEB作为落地示例。
---

## 摘要
我们提出 BLInD（Blind Learned Intent Distribution，盲学习意图分布），这是一个紧凑网络，可将近期车辆状态历史（如速度、曲率、转向灯和车辆类型）映射为未来自车轨迹的 top-k 分布，且不使用摄像头、LiDAR、地图或目标轨迹输入。我们发现，仅凭车辆状态历史就足以学习近短期自车轨迹的有用多模态分布，且其低延迟特性使其非常适合安全关键场景的部署。我们研究了两种分布架构：自回归（AR）和流匹配（flow-matching），并在混合平台开源数据集和 Wayve 数据集上进行训练。二者均无需针对特定数据集进行适配即可泛化；流匹配模型在 Wayve 上取得最佳 top-k ADE/FDE 为 0.15/0.37 m，在 Waymo 上为 0.15/0.36 m，在 nuScenes 上为 0.28/0.59 m，而 AR 模型达到可比的覆盖度。将这些分布集成到 AEB 触发任务中时，与具有 100% 真阳性得分的 1-CTRV 策略相比，严格的全候选策略在 AR 下将假阳性从 1.51% 降至 0.11%（降低 13.7 倍，TP 为 94.9%），在流匹配下降至 0.06%（降低 25.1 倍，TP 为 98.7%）。BLInD 在 NVIDIA DRIVE Orin ECU 上使用 AR 头运行时间为 0.87 ms，使用流匹配头为 2.9 ms，使其兼容汽车 ECU 上的实时部署。现有的学习型分布模型依赖场景上下文，而盲车辆状态模型通常坍缩为单一路径；相比之下，BLInD 同时具备学习型、盲式和跨域特性，这一组合此前工作尚未展示。这些结果表明，此类分布为下游系统提供了可控且合理的意图采样接口，AEB 是其中一种实例化。

## Abstract
We present BLInD (Blind Learned Intent Distribution), a compact network that maps recent vehicle-state history (e.g. speed, curvature, indicator, and vehicle type) to a top-k distribution of future ego trajectories, with no camera, LiDAR, map, or object-track inputs. We find that vehiclestate history alone is sufficient to learn a useful multimodal distribution over near-term ego trajectories, and its low-latency nature makes it well-suited for safety-critical deployment. We investigate two distribution architectures, autoregressive (AR) and flow-matching, and train on both mixed-platform opensource and Wayve datasets. Both generalize without datasetspecific adaptation; the flow-matching model achieves best topk ADE/FDE of 0.15/0.37 m on Wayve, 0.15/0.36 m on Waymo, and 0.28/0.59 m on nuScenes, with the AR model reaching comparable coverage. Integrating the distributions into an AEB trigger task, a strict all-candidates policy reduces false positives from 1.51% to 0.11% with AR (13.7x reduction, 94.9% TP) and to 0.06% with flow-matching (25.1x reduction, 98.7% TP) compared to a 1-CTRV policy with 100% true positive score. BLInD runs in 0.87 ms with the AR head and 2.9 ms with the flow-matching head on an NVIDIA DRIVE Orin ECU making it compatible with real-time deployment on automotive ECUs. While existing learned distribution models rely on scene context and blind vehicle-state models typically collapse to a single path, BLInD is learned, blind, and cross-domain simultaneously, a combination not demonstrated by prior work. These results show that such a distribution provides a controllable and plausible intent sampling interface for downstream systems, with AEB as one instantiation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：ADAS/自动驾驶中的安全协同驾驶需要预测人类驾驶员近期意图。传统方法常用单一确定性外推（如 CTRV）或启发式轨迹混合，但未来自车轨迹本质上是多模态的。
- **背景矛盾**：
  - 现有学习式多模态轨迹预测多依赖摄像头、LiDAR、HD 地图、目标轨迹等场景上下文。
  - 仅依赖车辆状态的“盲”模型虽低延迟，但通常退化为单一路径，无法提供可查询的未来分布。
- **论文含义**：BLInD 尝试证明，仅凭近期车辆状态历史（速度、曲率、转向灯、车型）即可学习未来自车轨迹的 top-k 分布，并作为轻量级 ego-dynamics prior 供下游系统查询。其目标不是替代感知丰富型规划器，而是提供低延迟、跨平台、可控的意图采样接口，AEB 是其中一个实例。

## 2. 方法论

- **核心输入输出**：
  - 输入：车辆状态历史 \(H\)，包括速度、后轴曲率、车辆类型、转向灯状态。
  - 输出：未来自车轨迹分布 \(p(\tau_{1:K}|H)\)，每个候选 \(\tau_k\) 包含 \(T=10\) 个未来路点，间隔 0.2 秒，即约 2 秒未来。
  - 明确不使用摄像头、LiDAR、地图、目标轨迹或车道特征。
- **紧凑网络结构**：
  - 每个车辆状态变量嵌入为 64 维，经过 4 个轻量自注意力块，每块 4 个注意力头。
  - 在共享编码器上接两种分布头：自回归头（AR）和流匹配头（flow-matching）。
- **自回归头（AR）**：
  - 将驾驶员意图建模为未来速度与曲率序列的模式条件分布。
  - 先预测 \(M\) 个高层意图模式的类别分布 \(p_\theta(z_{1:M}|H)\)。
  - 每个模式使用轻量 GRU 解码器，逐步预测下一速度 bin 和条件于速度的曲率 logits。
  - 速度离散为 64 个均匀 bin，范围 \([0,40]\) m/s；曲率离散为 256 个非均匀 bin，范围 \([-0.25,0.25]\) m\(^{-1}\)，近零曲率分辨率更细。
  - 训练时使用教师强制，以前一步真实速度/曲率 bin 为输入，采用交叉熵损失。
  - 解码所有模式后，选择轨迹重建损失最低的模式，只回传该模式的速度/曲率损失，并用该模式作为辅助模式分类交叉熵标签。
  - 推理时选择 top-k 模式，对每个模式贪心解码 T 步，得到排序候选轨迹。
- **流匹配头（flow-matching）**：
  - 将意图建模为未来 ego-frame 路点上的连续分布，学习条件速度场。
  - 10 个带噪未来路点投影为 64 维 token，经过两个交叉/自注意力块，条件于车辆历史 token，输出 2D 速度更新。
  - 训练时把真实未来轨迹视为干净样本，从高斯噪声采样基态，随机采样插值时间，构造中间带噪轨迹；模型预测从基态到真实轨迹的常数 rectified-flow 速度，使用 MSE 损失。
  - 每个真实未来轨迹采样多组时间/噪声对，使流场在噪声到数据的多个点上受监督。
  - 推理时 top-1 从零基态积分，固定历史下确定性；额外 \(k-1\) 个候选从高斯基态积分生成。所有候选使用 10 步 Euler 积分。
  - 由于流采样器不按似然排序候选，下游查询对生成轨迹赋均匀权重。

## 3. 实验设计

- **数据集与场景**：
  - 开放环评估：Wayve 内部 split、Waymo、nuScenes。
  - 训练数据：开源混合数据（Waymo、nuScenes）加 Wayve 内部数据。
  - AEB 下游评估：两个内部 AEB 数据集。
    - 真阳性集：228 个 NCAP 风格碰撞场景。
    - 假阳性集：约 98,000 个 targeted non-collision 样本，用于压力测试轨迹型 AEB 逻辑。
- **Benchmark 与指标**：
  - 开放环：top-k ADE、FDE、miss rate，miss 阈值 2 m；k 取 1、5、10。
  - AEB：TP score 与 FP rate；k 取 1、5、10。
- **对比方法**：
  - CTRV top-k：固定速度，围绕测量曲率生成 \(\pm 0.08\) m\(^{-1}\) 内的曲率假设；第一候选为 nominal CTRV，后续为对称左右曲率偏移。
  - AR OS mix w/ ind.：在开源混合数据和 Wayve 内部数据上训练的 AR 模型。
  - Flow OS mix w/ ind.：在同样数据上训练的流匹配模型。
- **AEB 规则**：
  - 每个候选轨迹沿预测路点扫掠自车 footprint，与固定未来环境做重叠检查。
  - 当查询候选集中所有轨迹均碰撞时触发 AEB。
  - 目标、静态场景、未来占用/碰撞几何对所有方法固定，以保证比较公平。
- **消融趋势**：
  - 加入开源混合数据显著改善 Waymo/nuScenes 跨域覆盖。
  - 转向灯输入在当前 flow 运行中没有一致总体增益。
  -
