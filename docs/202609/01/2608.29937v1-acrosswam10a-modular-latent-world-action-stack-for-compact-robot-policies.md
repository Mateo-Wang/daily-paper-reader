---
title: "AcrossWAM1.0:A Modular Latent World-Action Stack for Compact Robot Policies"
title_zh: AcrossWAM1.0：用于紧凑机器人策略的模块化潜在世界-动作堆栈
authors: "Yafei Zhang, Nan Wu"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29937v1"
tags: ["query:robotics"]
score: 9.0
evidence: 面向紧凑机器人策略的潜在世界-动作模型，模块化潜在世界解码器与流匹配动作生成
tldr: "潜在世界-动作模型通过在特征空间预测动作相关子目标来避免渲染未来像素，但 LaWAM 原始实现将世界模型、多模态骨干和部署检查点紧密耦合。AcrossWAM1.0 将其模块化：策略适配器产出潜在动作和生成上下文，保留的潜世界解码器将预测转换锚定于当前场景，流匹配专家生成连续动作块，并将训练教师与推理图分离。在 2000 条 LIBERO 上，0.8B 的 Qwen3.5 达到 97.45% 成功率，2B 的 Qwen3-VL 为 98.00%，仅差 0.55 个百分点，且参数量减少 42.4%。这为紧凑型潜在世界-动作策略提供了可审计的软件和评估边界。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29937v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1393, \"height\": 772, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29937v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1359, \"height\": 1173, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29937v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1383, \"height\": 761, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29937v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1271, \"height\": 168, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29937v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1053, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29937v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1009, \"height\": 297, \"label\": \"Table\"}]"
motivation: LaWAM 的原始实现将潜世界模型、多模态主干与部署检查点紧密耦合，限制了紧凑策略的模块化扩展与可审计评估。
method: 采用策略适配器生成潜在动作与生成上下文，保留潜世界解码器锚定场景转换，并用流匹配专家输出连续动作块。
result: "LIBERO 上 0.8B 模型成功率 97.45%，对比 2B 的 98.00% 仅差 0.55 个百分点，参数减少 42.4%。"
conclusion: AcrossWAM1.0 提供了紧凑潜世界-动作策略的可审计模块边界与评估准则，并验证跨主干压缩可行性。
---

## 摘要
潜在世界-动作模型通过在特征空间中预测与动作相关的视觉子目标来避免渲染未来像素。LaWAM确立了这一表述，但其原始呈现将世界模型、多模态骨干网络和部署检查点紧密耦合。我们引入AcrossWAM1.0，这是对这一潜在世界-动作堆栈的模块化与规模扩展研究。我们不将潜在子目标作为新算法呈现，而是明确模块边界：策略适配器产生潜在动作和动作生成上下文；保留的潜在世界解码器将预测的转移扎根于当前场景；流匹配专家生成连续动作块。我们进一步将仅用于训练的教师网络与推理图分离，并提供可验证的部署导出。在2,000对LIBERO片段上，将Qwen3-VL-2B骨干替换为Qwen3.5-0.8B，成功率达到97.45%，而2B模型为98.00%（差异为-0.55个百分点；精确McNemar检验p=0.266）。这并不证明两者等价，但满足预先指定的两点保留标准。紧凑的、可推理到达的检查点包含1,472.6M个唯一参数，比原始2B策略少42.4%，同时所有保留的张量与源检查点逐位一致。此外，通过MiniCPM-V适配器冒烟测试检查了跨家族执行；闭环跨家族迁移仍是一个开放的评估问题。因此，AcrossWAM1.0为紧凑的潜在世界-动作策略贡献了可审计的软件和评估边界，有别于LaWAM原始的潜在子目标贡献。

## Abstract
Latent world-action models avoid rendering future pixels by predicting an action-relevant visual subgoal in feature space. LaWAM established this formulation, but its original presentation left the world model, multimodal backbone, and deployment checkpoint tightly coupled. We introduce AcrossWAM1.0, a modularization and scaling study of this latent world-action stack. Rather than presenting latent subgoals as a new algorithm, we make the module boundary explicit: a policy adapter produces latent-action and action-generation contexts; a retained latent world decoder grounds the predicted transition in the current scene;and a flow-matching expert generates continuous action chunks. We further separate training-only teachers from the inference graph and provide a verifiable deployment export. On 2,000 paired LIBERO episodes, replacing a Qwen3-VL-2B backbone with Qwen3.5-0.8B yields 97.45% success versus 98.00% for the 2B model (a-0.55percentage-point difference; exact McNemarp=0.266). This does not prove equivalence, but it meets a prespecified two-point retention criterion. The compact, inference-reachable checkpoint contains 1,472.6M unique parameters, 42.4% fewer than the original 2B policy, while all retained tensors are bitwise identical to the source checkpoint. Cross-family execution is additionally checked with a MiniCPM-V adapter smoke test; closed-loop cross-family transfer remains an open evaluation. AcrossWAM1.0 therefore contributes an auditable software and evaluation boundary for compact latent world-action policies, distinct from LaWAM's original latent-subgoal contribution.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：机器人操作策略需要同时关联语义意图、视觉状态与连续控制。视觉-语言-动作（VLA）模型虽有强语义表征，但动作生成通常缺乏对“动作引起的未来状态变化”的显式预测。世界-动作模型（WAM）显式预测场景应如何变化，可在不渲染像素的前提下，通过在特征空间中预测动作相关的视觉子目标来提升控制性能。
- **核心问题**：LaWAM 已提出“潜在世界-动作模型”的基本形式，但其原始实现将世界模型、多模态骨干网络和部署检查点高度耦合，导致模块化、紧凑化和可审计性较差。本文并不宣称提出新的潜在子目标算法，而是关注一个更窄的系统问题：**如何将潜在世界-动作计算模块化、紧凑化，并使之成为一种可审计、可部署的策略堆栈**。
- **整体含义**：AcrossWAM1.0 是对 LaWAM 算法的“软件工程与评估边界”重定义，强调模块边界、骨干适配、训练/推理图分离、紧凑骨干缩放和精确部署导出，而非算法层面上的新贡献。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在 LaWAM 的潜在子目标学习框架基础上，显式划分模块边界——策略适配器、潜在世界解码器、流匹配动作专家，并分离训练专用教师网络与推理可达图。
- **总体概率模型**（公式 1）：
  \[
  p(a, \hat{h}_{t+\Delta}, z_t | o_t, \ell, s_t) =
  p_\theta(z_t | o_t, \ell)\,
  p_\omega(\hat{h}_{t+\Delta} | h_t, z_t)\,
  p_\eta(a | c_\theta, h_t, \hat{h}_{t+\Delta}, s_t)
  \]
  其中 \(h_t = f_\psi(o_t)\) 为冻结的 DINOv3 特征；\(z_t\) 为 32 维潜在动作；\(\hat{h}_{t+\Delta}\) 为潜在视觉子目标；动作由流匹配专家 \(p_\eta\) 生成。

- **三阶段流程**：
  - **Stage 1：潜在世界模型学习**。使用冻结的 DINOv3 提取当前/未来帧特征；逆动力学 Q-Former 编码转移对得到潜在动作 \(z_t\)；潜在世界解码器 \(D_\omega\) 用 \(z_t\) 调制当前 token，预测未来特征 \(\tilde{h}_{t+\Delta}\)，并使用未来特征监督和状态位移监督。损失为：
    \[
    L_{S1} = \text{SmoothL1}(\tilde{h}_{t+\Delta}, h_{t+\Delta}) + \lambda_s \| m_s \odot (g_\xi(s_t,z_t)-\Delta s_t)\|_2^2 + \beta_{KL} D_{KL}(q_\phi \| \mathcal{N}(0,I))
    \]
  - **Stage 2：策略学习**。多模态骨干（Qwen 系列）接收指令和图像，通过 8 个“ACT 查询”产生潜在动作上下文，经单查询交叉注意力映射器 \(M_\rho\) 得到预测潜在动作 \(\hat{z}_t\)；蒸馏损失对齐教师 \(z_t^*\)；再用保留的世界解码器生成潜在子目标，并由子目标条件流匹配专家生成动作块。损失为：
    \[
    L_{S2} = L_{flow} + \lambda_p L_{subgoal} + \lambda_d L_{distill}
    \]
    其中 \(L_{flow}\) 为流匹配回归损失，\(L_{subgoal}\) 为潜在子目标特征重构损失。
  - **推理与部署导出**：推理图仅保留骨干、潜在映射器、世界解码器和流匹配专家，移除逆动力学教师、状态预测头等训练专用模块；导出后检查点逐张量进行位级验证。

- **三种证据层级**：界面兼容（adapter 可执行 I/O 契约）、闭环骨干缩放（同一契约下不同骨干保留性能）、冻结跨家族迁移（本文明确不声称）。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集与场景**：
  - 主要 closed-loop 实验：LIBERO 基准（Long / Object / Goal / Spatial 四个套件，每套 10 个任务 × 50 次试验 = 500 个片段，两策略共 2,000 个配对片段）。
  - 辅助诊断：RoboTwin 任务上的语言 grounding 左/右诊断实验。
  - 跨家族适配器验证：MiniCPM-V 4.6 的前向/反向冒烟测试。
- **对比方法**：与近期 VLA 和 WAM 系列论文（如 π0、OpenVLA、π0.5、UniVLA、VLA-JEPA、Motus、Cosmos-Policy、LaWAM、Fast-WAM、SelfWAM 等）在 LIBERO 和 RoboTwin 2.0 上比较成功率和参数量；但表格只是外部参考，非本人复现。
- **内部受控对比**：同一模块堆栈下 Qwen3-VL-2B 与 Qwen3.5-0.8B 的 paired 比较，使用相同种子、任务顺序和片段键。

## 4. 资源与算力

- **论文未明确报告** GPU 型号、数量或具体训练时长、显存和能耗数据。
- 仅提到紧凑模型训练 25,000 步，最终训练损失 0.01428，验证 MSE 0.002313；未给出训练墙钟时间或硬件配置。
- 此外，文中坦承缺少紧凑模型的延迟、峰值内存和导出的加速器/仿真器 smoke test，属于明确局限。

## 5. 实验数量与充分性

- **实验数量概览**：
  - 闭环骨干缩放：2,000 配对 LIBERO 片段（每策略），四个套件独立统计。
  - 部署导出验证：1,078 个保留张量位级比对、321 个训练专用键（411.1M 参数）移除、718 个别名键重生成验证。
  - 适配器验证：MiniCPM-V 一次前向/反向冒烟测试。
  - 语言 grounding 诊断：两个 RoboTwin 任务，左/右冲突指令，比较标准和 CFG 变体。
- **充分性评估**：
  - **优点**：配对实验设计、同一评估协议、明确统计检验（McNemar）、预设保留标准（≤2 个百分点）、精确参数计数，客观性较强。
  - **不足**：每个骨干仅一个训练检查点，无法衡量训练种子方差；闭环覆盖仅限于 Qwen 家族和 LIBERO；RoboTwin 与跨家族均无闭环结果；语言诊断只含两个任务且显示出明显的 adherence–completion 权衡。综合来看，实验足以支撑“模块化与紧凑缩放”这一有限主张，但不足以支撑“普适可移植”或“统计等价”类强结论。

## 6. 论文的主要结论与发现

- **紧凑缩放成立**：Qwen3.5-0.8B 在 2,000 个 LIBERO 片段上成功率为 97.45%，Qwen3-VL-2B 为 98.00%，相差 −0.55 个百分点；精确 McNemar p=0.266，未检测到配对差异，满足预设的“≤2 点”保留标准，但作者明确不声称统计等价。
- **部署导出可验证**：紧凑检查点唯一可推理参数为 1,472.6M 个，比源 2B 策略减少 42.4%；导出验证中所有保留张量与源检查点逐位一致，训练专用状态被完全移除。
- **适配器抽象有效**：Qwen 家族内闭环缩放可行；MiniCPM-V 冒烟测试表明界面兼容可执行，但冻结跨家族迁移仍无闭环证据。
- **语言介入仍有风险**：更强的语言 CFG 可提高左/右指令遵循（27.4% → 50.3%），但同时增加非冲突指令下的任务崩塌率（1% → 10%），说明潜在世界-动作策略的语言-动作解耦仍需改进。
- **总体结论**：AcrossWAM1.0 提供了紧凑潜在世界-动作策略的可审计模块边界与评估准则，验证了跨骨干压缩的可行性，但应明确区分离“已证实的紧凑缩放”与“尚未证实的冻结可移植性”。

## 7. 优点

- **模块化设计清晰**：显式定义了“骨干适配器-潜在世界解码器-动作专家”的契约，包括查询所有权、隐藏层对齐、冻结/训练边界、训练与部署模块分离，便于工程落地和审计。
- **可审计与验证严谨**：提供可复现的配对评估、精确 McNemar 检验、预设保留标准、张量级位验证、训练/部署参数统计，避免了“图上模块化但代码耦合”的问题。
- **诚实区分证据层级**：将“界面兼容”、“闭环骨干缩放”与“冻结跨家族迁移”区分开，主动声明未声称的结论，减少过度推广。
- **紧凑性显著**：以几乎无损的性能将可部署参数量减少 42.4%，对资源受限场景具有实用价值。
- **定性可视化补充**：用辅助 RGB 解码器展示潜在预测的长期行为，虽不用于策略，但可帮助理解潜在子目标接口。

## 8. 不足与局限

- **实验覆盖有限**：封闭环境仅 LIBERO；无真实机器人、无 RoboTwin 闭环、无跨家族闭环结果；每个骨干仅一个检查点，无法评估训练种子方差。
- **缺少部署性能数据**：紧凑模型的延迟、峰值内存、推理吞吐未报告；导出的检查点尚未做加速器和仿真器级 smoke test。
- **跨家族迁移未验证**：紧凑实验中世界解码器被微调，DINOv3 和教师冻结，但 `Dω`、VLM、mapper、动作专家均被优化，因此不能证明“一个冻结世界解码器跨不同骨干族迁移”成立。
- **语言 grounding 问题明显**：诊断仅两个任务，且 CFG 提升左/右遵循的同时恶化非冲突指令下任务完成率，说明语言条件化能力弱，有实际部署风险。
- **统计结论保守但可能被误读**：p=0.266 只表示未检测到差异，并非等价；若外界引用为“等价”，会产生过大解读。
- **基准数字转录存在不可比风险**：Table 1 中外部方法结果来自各自论文，训练配方、参数计数和评估实现不同，仅作外部上下文，不能严格排名。RoboTwin 列的 AcrossWAM1.0 数字来自何处未在本段展开，需谨慎对待。

（完）
