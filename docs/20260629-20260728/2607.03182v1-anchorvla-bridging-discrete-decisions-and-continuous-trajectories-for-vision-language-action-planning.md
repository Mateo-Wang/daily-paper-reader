---
title: "AnchorVLA: Bridging Discrete Decisions and Continuous Trajectories for Vision-Language-Action Planning"
title_zh: AnchorVLA：连接离散决策与连续轨迹的视觉-语言-动作规划
authors: "Qi Liu, Yabei Li, Hongsong Wang, Heng Zhang, Lei He"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03182v1"
tags: ["query:ar"]
score: 8.0
evidence: 提出AnchorVLA，连接离散决策与连续轨迹，用于自动驾驶规划
tldr: "现有VLA规划器在连续轨迹生成方面存在局限，如弱约束或依赖低信息密度坐标token导致语义-动作对齐困难与推理效率低下。本文提出AnchorVLA分层决策锚定框架，通过轨迹模式锚点作为高层VLA推理与连续轨迹执行的显式接口，利用决策即锚表示编码行为级运动模式，并在锚定义残差空间生成细粒度连续轨迹。在Bench2Drive闭环基准上取得77.28%成功率和89.92驾驶得分，实现SOTA性能。该方法显著提升推理效率、语义-动作对齐与连续生成灵活性，为自动驾驶规划提供了高效且可解释的范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03182v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1366, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03182v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1344, \"height\": 911, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03182v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1376, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03182v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1433, \"height\": 1044, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03182v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1433, \"height\": 515, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03182v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 740, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03182v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03182v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1236, \"height\": 688, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03182v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1449, \"height\": 213, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03182v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1450, \"height\": 209, \"label\": \"Table\"}]"
motivation: 现有VLA规划器难以对齐高层语义与连续轨迹，且因低效的坐标生成导致推理瓶颈。
method: AnchorVLA将行为决策编码为轨迹模式锚token，在锚定义残差空间中生成连续轨迹。
result: "在Bench2Drive闭环评测中，成功率77.28%，驾驶得分89.92，均达SOTA。"
conclusion: 通过决策锚定接口平衡离散推理与连续生成，提升效率、对齐与灵活性。
---

## 摘要
自主驾驶规划需要将导航意图、交通规则、动态交互和语言指令转换为可执行的连续轨迹。视觉-语言-动作模型已被引入驾驶规划，以改善长尾泛化、常识推理、高层语义理解和可解释性。然而，现有的VLA规划器主要采用基于规划头的轨迹预测或全轨迹自回归生成。前者仅通过VLA推理弱约束连续轨迹生成，而后者依赖于长序列的低信息密度坐标标记，使得语义-动作对齐困难，并导致离散化误差和低效推理。为解决这些限制，我们提出AnchorVLA，一种层次化的决策锚定VLA规划框架，使用轨迹模式锚点作为高层VLA推理与连续轨迹执行之间的显式接口。具体而言，决策即锚点表示用锚点标记表示行为级驾驶决策，每个锚点编码整个局部运动模式而非单个坐标点；决策锚定残差流则在所选锚点定义的残差空间中生成细粒度连续轨迹，在高层决策后捕获多模态执行细化。通过在紧凑且语义有意义的锚点上进行推理，而非自回归生成航点序列，AnchorVLA保留了基于LLM的决策能力，同时提高了推理效率、语义-动作对齐和连续生成灵活性。在Bench2Drive闭环基准上的实验表明，AnchorVLA达到了77.28的最新成功率和89.92的具有竞争力的驾驶分数。

## 速览
**TLDR**：当前视觉-语言-动作（VLA）规划器在自动驾驶中面临语义-动作对齐难和推理效率低的问题。本文提出AnchorVLA，通过轨迹模式锚作为高层决策与连续轨迹间的显式接口，将行为级决策编码为紧凑的锚令牌，并在锚定义残差空间中生成细粒度轨迹。在Bench2Drive闭环测试中，AnchorVLA以77.28%成功率和89.92驾驶得分刷新SOTA。该框架显著提升语义与动作的对应性及生成灵活性，为VLA规划提供了高效可解释的新范式。 \
**Motivation**：现有VLA规划器仅弱约束连续轨迹生成或依赖低信息密度坐标序列，导致离散化误差和推理低效。 \
**Method**：AnchorVLA使用决策即锚表示，将驾驶决策编码为局部运动模式令牌，并通过决策锚定残差流在残差空间生成连续轨迹。 \
**Result**：在Bench2Drive上达到77.28%成功率和89.92驾驶得分，取得SOTA性能。 \
**Conclusion**：AnchorVLA通过锚定决策与轨迹实现了高效的语义-动作对齐与推理，验证了层次化VLA规划的有效性。

---

## Abstract
Autonomous driving planning requires translating navigation intent, traffic rules, dynamic interactions, and language instructions into executable continuous trajectories. Vision-Language-Action models have been introduced into driving planning to improve long-tail generalization, commonsense reasoning, high-level semantic understanding, and explainability. However, existing VLA planners mainly follow planning-head-based trajectory prediction or full-trajectory autoregressive generation. The former only weakly constrains continuous trajectory generation with VLA reasoning, while the latter relies on long sequences of low-information-density coordinate tokens, making semantic-action alignment difficult and leading to discretization errors and inefficient inference. To address these limitations, we propose AnchorVLA, a hierarchical decision-anchored VLA planning framework that uses trajectory-pattern anchors as an explicit interface between high-level VLA reasoning and continuous trajectory execution. Specifically, Decision-as-Anchor Representation represents behavior-level driving decisions with anchor tokens, each encoding an entire local motion pattern rather than a single coordinate point. Decision-Anchored Residual Flow then generates fine-grained continuous trajectories in the selected anchor-defined residual space, capturing multi-modal execution refinements after high-level decision making. By reasoning over compact and semantically meaningful anchors instead of autoregressively generating waypoint sequences, AnchorVLA preserves LLM-based decision making while improving inference efficiency, semantic-action alignment, and continuous generation flexibility. Experiments on the Bench2Drive closed-loop benchmark show that AnchorVLA achieves a state-of-the-art Success Rate of 77.28 and a competitive Driving Score of 89.92.

---

## 论文详细总结（自动生成）

# 论文总结：AnchorVLA—连接离散决策与连续轨迹的视觉-语言-动作规划

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：自主驾驶规划需要将导航意图、交通规则、动态交互和语言指令转换为可执行的连续轨迹。Vision-Language-Action（VLA）模型被引入以提升长尾泛化、常识推理、高层语义理解与可解释性。
- **现有方法的局限**：
  - **规划头法**（如ORION、SimLingo）：LLM/VLM编码的特征仅作为条件，弱约束连续轨迹生成，导致局部动作与高层决策难以对齐。
  - **完整轨迹自回归生成法**（如AutoVLA、LinkVLA）：将轨迹离散化为坐标或航点令牌，每个令牌只对应低维几何点，信息密度低，导致长序列、离散化误差累积、语义-动作对齐困难、推理效率低下。
- **核心问题**：高层语言推理与低层连续轨迹执行之间存在“抽象鸿沟”，现有方法无法高效、可解释地桥接这一鸿沟。
- **本文目标**：提出AnchorVLA，通过**轨迹模式锚（trajectory-pattern anchors）** 作为显式的决策接口，将行为级决策与连续轨迹生成分离，实现高效、可对齐、灵活的多模态规划。

## 2. 方法论：核心思想、关键技术细节、公式与算法流程

### 2.1 核心思想
- **整体架构**：将全局轨迹分布 \( p(\tau|x) \) 分解为两步：
  \[
  p(\tau|x) = \sum_{k=1}^{K} p(a_k|x) \cdot p(r_k|x, a_k),\quad r_k = \tau - a_k
  \]
  其中 \( a_k \) 为轨迹模式锚（完整局部运动模式），\( r_k \) 为锚定义残差空间中的连续残差。
- **关键洞察**：锚不仅仅是“条件”，更是定义了后续残差生成的坐标系和局部行为空间，从而将高层决策（选择模式）与低层执行（细化轨迹）解耦。

### 2.2 关键技术细节

#### ① Decision-as-Anchor Representation (DAAR)
- **轨迹模式码本构造**：对训练轨迹进行 k-means 聚类，得到 \( K=100 \) 个锚，每个锚表示一个完整运动模式。
- **软锚目标**：对每个真实轨迹 \( \tau_{gt} \)，计算其与各锚的 L2 距离，取 top-N 近邻，用温度系数 \( \gamma \) 生成软标签 \( q_k \)（式 4）。
- **两种实现方式**：
  - **查询式建模（Query-based）**：在VLM输入序列中附加可学习查询令牌，经LLM编码后用MLP头输出锚分布，通过KL散度训练。
  - **自回归式建模（Autoregressive）**：为每个锚引入一个特殊令牌（锚令牌），附在“控制令牌”之后，将锚预测转化为LLM的next-token预测，但损失函数仍用软锚目标（式 8）。推理时，模型只输出少量锚令牌，避免冗长的坐标序列生成。

#### ② Decision-Anchored Residual Flow (DARF)
- **残差空间流匹配**：对每个候选锚 \( a_k \)，定义残差目标 \( r_k^{gt} = \tau_{gt} - a_k \)，在残差空间中执行流匹配：
  \[
  z_k^t = (1-t)\epsilon + t r_k^{gt},\quad v_k^* = r_k^{gt} - \epsilon
  \]
  通过Euler积分得到去噪后的残差，最终轨迹 \( \hat{\tau}_k = a_k + z_k^1 \)。
- **多候选机制**：DAAR 输出 top-M 锚（M=6），DARF 为每个锚生成候选轨迹，并预测置信度分数。仅对与真实轨迹最接近的锚计算流匹配损失（式 11），并训练置信度（式 12）。推理时选择置信度最高的候选。
- **解码器架构**：包含速度分支（输入噪声残差、时间、锚特征及VLM上下文，通过交叉注意力预测速度场）和互不耦合的置信度分支（仅基于锚特征和上下文预测标量置信度）。

### 2.3 训练流程
1. **第一阶段**：固定VLM骨干？论文说“先训练锚决策模型15 epoch，再冻结VLM骨干训练DARF”。但正文中“Training”部分描述为：先训练DAAR锚决策模型15 epoch（8×A100，batch size 16），然后冻结VLM骨干，单独训练DARF 15 epoch（4×A100，batch size 32）。说明VLM骨干在第二阶段被冻结。

## 3. 实验设计

### 3.1 数据集与基准
- **训练数据**：遵循SimLingo [14] 的设置，包含专家轨迹、指令条件梦想轨迹以及混合语言监督（VQA、驾驶评论、无语言样本）。
- **评测基准**：**Bench2Drive**（基于CARLA的闭环基准），报告指标包括：
  - Driving Score (DS)
  - Success Rate (SR)
  - Efficiency
  - Comfort
  - Multi-Ability（Merging, Overtake, Brake, Give-Way, Traffic-Sign）

### 3.2 对比方法
- **非VLA方法**：TCP-traj, ThinkTwice, DriveAdapter, AD-MLP, UniAD-Base, VAD, DriveTransformer, DiffusionDrive, BridgeDrive, TransFuser++。
- **VLA方法**：Orion-VAE, Orion-Diffusion, AutoVLA, SimLingo, LinkVLA。其中SimLingo作为AnchorVLA的VLM骨干。

### 3.3 消融实验设置
- **决策建模消融**：无锚决策（直接所有锚进DARF） vs. 查询式 vs. 自回归式 vs. LinkVLA-AR（全轨迹自回归基线）。
- **流匹配消融**：确定性残差回归 vs. 全轨迹流匹配（Full Flow） vs. DARF。
- **导航模态消融**：GPS目标点 vs. 导航指令。

## 4. 资源与算力

- **文中明确说明**：
  - 锚决策模型（DAAR）训练：8×NVIDIA A100 GPU，batch size 16，15 epoch。
  - 残差流模型（DARF）训练：4×NVIDIA A100 GPU，batch size 32，15 epoch。
  - VLM骨干：InternVL2-1B（InternViT-300M + Qwen2-0.5B-Instruct），在SimLingo基础上修改规划接口。
- **未说明**：总训练时长、数据量大小、推理时算力要求（仅给出额外延迟：自回归式锚预测+ DARF共约82ms？表3中自回归额外延迟64ms，但DARF表4中额外延迟18ms，合计合理）。

## 5. 实验数量与充分性

- **主结果**：1张主表（表1），对比11种方法，报告4项指标。
- **多能力评估**：1张表（表2），对比5种子能力（Merging, Overtake, Brake, Give-Way, Traffic-Sign）及均值，8种方法。
- **消融实验**：
  - 决策建模消融（表3）：4个 variant，对比DS, SR, Efficiency, Comfort，含额外延迟。
  - 流匹配消融（表4）：3个 variant，对比4个指标。
  - 导航模态消融（表5）：2种模态，对比闭环指标和多能力均值。
- **可视化与失败分析**：附录A提供定性结果和三类失败案例（图4、图5）。
- **充分性评价**：
  - **优点**：涵盖了核心设计点的消融，对比了不同层次的基线（无锚、查询、自回归、全轨迹自回归），验证了DARF相对确定性/全流匹配的优势，以及不同导航模态的鲁棒性。
  - **不足**：
    - 未提供在不同场景（如夜间、雨天、非结构化道路）下的细粒度拆解。
    - 未与其他VLA方法做公平的训练数据/骨干消融（如与LinkVLA在完全相同的骨干和训练设置下比较，仅改变规划接口）。文中AnchorVLA基于SimLingo骨干，而LinkVLA使用PDM-Lite专家，骨干差异可能影响公平性。
    - 缺少对锚数量K、温度γ、流匹配步数等超参数的充分分析（文中固定K=100, M=6，流步数未明说但表4默认相同步数？）。

## 6. 论文的主要结论与发现

1. **SOTA性能**：AnchorVLA在Bench2Drive闭环基准上达到 **77.28% 成功率（SR）** 和 **89.92 驾驶分（DS）**，SR在全方法中最高，DS仅次于LinkVLA（91.01）但差距很小。
2. **高效推理**：自回归锚预测仅需额外64ms延迟（相比全轨迹自回归的361ms），证明紧凑锚令牌的推理优势。
3. **决策-执行解耦有效**：完全移除锚决策（所有锚进DARF）使SR下降至70.91%；查询式锚决策提升至73.81%；自回归式锚决策最佳（77.28%），说明LLM原生令牌接口更适合行为级决策。
4. **残差空间流匹配优于全轨迹流匹配**：在相同流步数下，DARF（SR 77.28%）远超全轨迹流匹配（72.73%）和确定性残差（70.45%）。
5. **多能力均衡**：在Merging、Overtake、Brake等细粒度能力上取得较好平均分（74.22%），综合表现最佳。

## 7. 优点

- **创新性框架设计**：首次将轨迹模式锚作为VLA中离散决策与连续轨迹的显式接口，解决了现有方法在对齐与效率上的矛盾。
- **高效的语义-动作对齐**：锚令牌编码完整运动模式，每个令牌信息密度高，使得LLM能够基于语义进行行为级推理，而非逐点坐标生成。
- **生成灵活性**：DARF在锚定义残差空间中利用流匹配捕获多模态细化，兼顾了行为一致性（锚约束）和执行灵活性（残差可多模式）。
- **公平的骨干使用**：基于公开SimLingo骨干，并公开训练配置，便于复现和对比。
- **全面的消融与可视化**：从决策建模、流匹配、导航模态三个角度设计消融，附录提供定性分析和失败案例，有助于理解方法行为。

## 8. 不足与局限

- **实验覆盖广度有限**：只在Bench2Drive闭环上评测，未涉及其他驾驶基准（如nuScenes open-loop, CARLA Leaderboard等）。未涵盖不同天气、光照、城市等场景的细粒度分析。
- **公平性隐患**：与LinkVLA等对比时，训练数据/专家策略（SimLingo vs PDM-Lite）和VLM骨干（InternVL2-1B vs 未知）可能不同，导致性能比较不够公平。作者仅修改SimLingo的规划接口，但未与SimLingo本身直接对比具有相同骨干的规划头或自回归基线（仅对比了LinkVLA-AR这一简化版本）。
- **可解释性未深入量化**：虽然宣称锚提供可解释的行为级决策，但仅展示了定性可视化，未定量衡量决策与语言指令的一致性，也未评估锚选择的可解释性或可纠正性。
- **舒适度较低**：Comfort分数（28.94）低于部分基线（如LinkVLA 34.62），说明生成轨迹的平滑性不足，可能产生急转向或急加速等不舒适行为，限制了实际部署。
- **锚聚类与残差先验的限制**：锚质量依赖于聚类训练轨迹的分布，若遇到分布外轨迹（如紧急避障），可能缺乏匹配的锚；残差模型只能在锚附近生成，无法完全纠正错误锚。
- **超参数敏感性未知**：未对锚数量K、候选数M、温度γ、流步数等关键超参数进行系统实验，最优值可能存在场景依赖。

（完）
