---
title: "ParallelWorld: Test-Time Scaling for Embodied Reasoning"
title_zh: ParallelWorld：具身推理的测试时扩展
authors: "Min Chen, Shengjun Zhang, Yuxin Li, Zhang Zhang, Xin Fei, Chong Xia, Yueqi Duan"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.22971v1"
tags: ["query:robotics"]
score: 8.0
evidence: 面向具身推理的测试时扩展与并行探索
tldr: 现有具身推理的测试时扩展常采用单步贪心搜索，难以处理遮挡环境下延迟反馈。ParallelWorld提出多视界树搜索框架，并行模拟多步轨迹，验证器动态剪枝并筛选高信息增益路径，最终综合长视界结果选择最优动作。在ESI-Bench上显著提升主动感知与推理性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22971v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1677, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22971v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1689, \"height\": 704, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.22971v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 1324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22971v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 423, \"label\": \"Table\"}]"
motivation: 现有主动推理方法缺乏长期规划，单步前瞻无法解决复杂环境中延迟反馈问题。
method: 提出ParallelWorld，用验证器引导的树搜索进行多步并行模拟，动态剪枝后选择最优动作序列。
result: 在ESI-Bench上，ParallelWorld持续改进了主动感知和推理的表现。
conclusion: 多视界前瞻模拟能有效提升具身推理的长时决策能力。
---

## 摘要
具身推理是具身智能的一项基本能力，是自主感知、推理及在物理环境中交互的基础。近期研究已将具身推理的范式从静态感知转向动态探索，即智能体通过与环境的交互获取任务相关信息。然而，现有的主动推理方法通常以递增方式生成探索轨迹，缺乏长时程规划。即使是近期出现的测试时扩展框架，也往往采用短视的单步前瞻，难以解决复杂、遮挡空间环境中固有的延迟反馈问题。为解决这一局限，我们提出了ParallelWorld，一种用于具身推理的多时域测试时扩展框架。不同于贪婪的单步尝试，ParallelWorld使智能体在采取行动之前，能够并行模拟和评估多步未来轨迹。具体而言，我们引入了一种验证器引导的树搜索范式。从当前状态出发，ParallelWorld分支为多条并行轨迹，并在多步时域内持续展开。在每个模拟步骤中，验证器智能体评估中间状态转移，动态剪除无前景的分支，并优先选择信息增益最高的路径。当多步前瞻仿真完成后，智能体综合长时程结果以决定最优动作序列。最后，答案智能体对所选轨迹进行推理以生成最终答案。在ESI-Bench上的大量实验表明，ParallelWorld持续提升了主动感知和推理性能。

## Abstract
Embodied Reasoning constitutes a fundamental capability of embodied intelligence, serving as the basis for autonomous perception, reasoning, and interaction within physical environments. Recent studies have shifted the paradigm of embodied reasoning from static perception toward dynamic exploration, where agents acquire task-relevant information through interactions with the environment. However, existing active reasoning approaches generally generate exploration trajectories incrementally without long-horizon planning. Even recently emerged test-time scaling frameworks often resort to myopic, single-step lookaheads, which struggle to resolve the delayed feedback inherent in complex, occluded spatial environments. To address this limitation, we propose ParallelWorld, a multi-horizon test-time scaling framework for embodied reasoning. Instead of greedy, single-step trials, ParallelWorld empowers agents to simulate and evaluate multi-step future trajectories in parallel before committing to an action. Specifically, we introduce a verifier-guided tree-search paradigm. Starting from the current state, ParallelWorld branches into multiple parallel trajectories and rolls them out continuously across a multi-step horizon. At each simulation step, a verifier agent evaluates the intermediate state transitions, dynamically pruning unpromising branches and prioritizing paths with the highest information gain. Once the multi-step prospective simulation is complete, the agent synthesizes the long-horizon outcomes to commit to the optimal action sequence. Finally, an answer agent performs reasoning over the selected trajectory to produce the final reasoning. Extensive experiments on ESI-Bench demonstrate that ParallelWorld consistently improves active perception and reasoning performance.

---

## 论文详细总结（自动生成）

# ParallelWorld：具身推理的测试时扩展（详细总结）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：具身智能体需要在物理环境中进行自主感知、推理和交互。传统方法多基于固定观测进行被动推理，无法处理遮挡、歧义和动态变化的环境。
- **范式转移**：近年研究将具身推理从静态感知转向“主动探索”，允许智能体通过与环境交互获取任务相关信息。但现有主动推理方法存在两个关键缺陷：
  - 探索轨迹是**逐步增量生成**的，缺乏对替代未来轨迹的长时程规划；
  - 已有的测试时扩展框架多采用**短视的单步前瞻**，无法有效应对复杂遮挡空间中的延迟反馈问题。
- **核心问题**：能否让具身智能体在真正执行前，通过模拟多条可能的未来轨迹来评估不同动作的后果，从而做出更优决策？
- **整体含义**：论文提出 **ParallelWorld**，一个多时域测试时扩展框架，通过并行模拟多个未来世界、验证器引导剪枝、以及基于最优路径的最终推理，显著提升具身推理中的主动感知与决策能力。

## 2. 提出的方法论

### 2.1 核心思想

- 将具身推理建模为**验证器引导的树搜索**过程：从当前可恢复的模拟器状态出发，同时展开多条候选轨迹，对未来多步进行前瞻评估，而非单步贪婪选择。
- 区分“内部前瞻搜索”与“最终实际执行”：内部可保留多个分支，但最终答案只基于一条物理一致的最优根到叶路径。

### 2.2 总体流程

- 输入：问题 \( q \)、初始状态 \( s_0 \)、初始观测 \( o_0 \)。
- 可执行动作空间：\( \mathcal{A} = \mathcal{A}_{cam} \cup \mathcal{A}_{task} \)，包括相机平移/旋转等动作和拾取、放置、倒水、堆叠等物理动作。
- 每一步维护一组保留轨迹 \( B_t = \{\tau_t^{(1)}, ..., \tau_t^{(K_t)}\} \)，每个轨迹对应一个独立可恢复的模拟世界。

### 2.3 关键组件与细节

- **状态表示**：每个分支运行时状态 \( \xi_t = (s_t, z_t, p_t, \omega_t) \) 包含模拟器状态、任务交互状态、相机姿态、相关物体状态。
- **前瞻扩展**：每个保留分支对所有可执行动作进行扩展，通过模拟转移 \( T \) 和渲染 \( O \) 得到新状态与观测，形成候选前沿 \( C_t \)。
- **验证器引导**：验证器 \( V_\phi \) 根据问题、参考观测、历史信息和候选证据评估每个候选的信息价值，偏好能揭示任务相关物体、消除视觉歧义、提供互补信息的分支。
- **分支宽度调度**：采用交替的扩展-收敛策略：奇数步保留多分支以维持多样性，偶数步收敛到单分支；按任务类别调整宽度，前期用较大宽度，后期用较小稳定宽度。
- **重放选中的动作**：由于物理动作会改变物体姿态和任务变量，验证后需从父快照重放动作，确保图像、物体状态和模拟器状态一致性。
- **Top-1 路径推理**：答案智能体仅接收最高排名根到叶路径上的后动作观测，保证观测来自物理一致的轨迹；通过从当前叶子回溯祖先链重建路径，避免拼接不同时间步的互不兼容观测。
- **置信度停止准则**：答案智能体输出预测、置信度和推理；当预测有效且置信度超过阈值 \( \gamma \) 时停止，否则最多探索到限定深度 \( L \)。
- **答案智能体与验证器分工**：验证器只返回候选分支标识，答案智能体负责输出最终任务答案、置信度和推理过程。

## 3. 实验设计

- **基准数据集**：使用 **ESI-Bench**（具身空间智能综合基准），包含 10 个任务类别、29 个子类别。因环境问题无法执行倒水动作，排除了 Liquid Volume 子类别，实际评估 28 个子类别。
- **对比方法**：
  - **Passive Single-View**：仅从初始位姿的单张观测预测答案（被动基线）。
  - **Active Exploration**：顺序选择相机或物理动作来收集证据后回答（主动基线）。
  - **ParallelWorld**：本文方法，使用同一答案模型骨干。验证器使用 GPT-5.5，答案智能体使用 GPT-5.4。
- **关键超参数**：最大探索深度 \( L = 15 \)，置信度阈值 \( \gamma = 0.8 \)。
- **主要实验内容**：
  - 在全部 28 个子类别上的定量对比（主结果）。
  - 定性案例分析：展示验证器如何在模拟未来世界中挑选信息性轨迹。
  - 消融实验：对比固定分支宽度 \( K=2,3,4 \) 与预定义 \( K \) 调度策略的精度、平均运行时间和探索步数。

## 4. 资源与算力

- **论文中未明确说明**具体的 GPU 型号、数量、训练时长或推理算力硬件细节。
- 文中仅提到使用 GPT-5.4（答案智能体）和 GPT-5.5（验证器智能体），并报告了**每问题平均运行时间**（如调度策略平均 350.6 秒/问题），但没有给出所使用的计算集群或能耗信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验覆盖 28 个子类别的精度对比，每个类别均有 Passive、Active、ParallelWorld 三列数据。
  - 消融实验在 5 个子类别（Rigid Containment、Partial Occlusion、Regional Boundary、Counting with Occlusion）上比较了 4 种配置（K=2、K=3、K=4、K Schedule）。
  - 提供了一个定性示例。
- **充分性与公平性**：
  - **优点**：所有主动方法使用相同答案模型骨干，对比条件相对公平；消融覆盖了不同任务类型并报告了精度、时间和步数三个维度，能较全面反映效率-精度的权衡。
  - **不足**：
    - 消融仅选择 5 个子类别，未覆盖所有 28 个子类别；
    - 定性分析仅一个案例，说服力有限；
    - 未提供多次运行的标准差或统计显著性检验；
    - 未与更复杂的测试时扩展方法（如基于学习的世界模型、MCTS 等）直接对比。

## 6. 主要结论与发现

- **ParallelWorld 在几乎所有子类别上均优于 Active Exploration**，尤其在时间理解和度量比较等需要长时程证据整合的任务上提升显著：
  - Unobserved Change：70.95% → **89.86%**（+18.91 个百分点）
  - Spatial Distance：58.55% → **67.11%**（+8.56 个百分点）
  - Action Order Inference：53.25% → **61.04%**
  - Agent Observation：48.87% → **62.41%**
- **预定义 K 调度策略效果最好**：平均准确率 59.56%，高于固定 K=2（55.52%）、K=3（50.72%）、K=4（51.87%），同时平均运行时间最低（350.6 秒/问题）。
- 增加固定分支宽度并非总能提升准确率，保留过多分支可能引入冗余或低信息量候选；交替扩展-收敛的调度能在多样性与搜索集中度之间取得更好平衡。
- 结论：**前瞻未来世界模拟是提升具身主动推理的有效机制**。

## 7. 优点

- **方法创新性强**：将测试时扩展从单步贪婪前瞻提升为多步并行树搜索，解决了延迟反馈问题。
- **设计合理且有实际考量**：区分“内部搜索”和“实际执行”，保证最终答案基于物理一致的轨迹，避免互相冲突证据。
- **验证器与答案智能体解耦**：角色分工明确，验证器负责剪枝和路径选择，答案智能体负责最终推理，符合可解释性和模块化原则。
- **效率-精度权衡良好**：通过分支宽度调度控制计算成本，在提升精度的同时降低平均运行时间。
- **实验覆盖任务类型广**：涉及遮挡、材料、物理动态、空间关系、认知映射、时间理解等 28 个子类别，较全面。

## 8. 不足与局限

- **计算成本高**：每个保留分支需对所有动作进行扩展，物理动作多或探索深度长时成本会显著增加。
- **依赖模拟器保真度和验证器可靠性**：模拟器与现实差距或验证器误判会直接影响探索质量。
- **信息丢失风险**：仅保留选择轨迹可能丢弃其他分支中的互补证据。
- **消融规模有限**：分支宽度消融只在 5 个子类别上完成，未给出统计显著性。
- **实验环境受限**：因环境问题排除“液体体积”子类别，未评估所有原始任务。
- **未考虑真实机器人部署**：当前实现基于可恢复的模拟器状态，尚未扩展到真实世界和动态环境；论文承认替换为学习世界模型是未来方向。
- **未报告算力资源详情**：无法评估复现成本。

（完）
