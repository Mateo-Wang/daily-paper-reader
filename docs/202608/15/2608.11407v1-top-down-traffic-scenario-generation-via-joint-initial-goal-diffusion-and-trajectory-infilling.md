---
title: Top-down Traffic Scenario Generation via Joint Initial-Goal Diffusion and Trajectory Infilling
title_zh: 通过联合初始-目标扩散与轨迹填充进行自上而下的交通场景生成
authors: "Da Saem Lee, Yash Vardhan Pant, Sebastian Fischmeister"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.11407v1"
tags: ["query:driving"]
score: 9.0
evidence: 通过初始-目标扩散和轨迹填充生成交通场景，支持自动驾驶仿真测试
tldr: "现有交通模拟器依赖已知初始状态生成轨迹，限制可扩展性和多样性，且初始化缺乏可解释性。本文提出TrafficDiffuser，联合扩散生成初始-目标状态对，将轨迹生成简化为填充问题，使场景更可解释并能与现有模型集成。在Argoverse 2上，速度分布距离降低55.3%，离路率降低2.8%。该方法提升了交通场景生成的质量与实用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11407v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 746, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11407v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1741, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11407v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 897, \"height\": 162, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11407v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 662, \"height\": 230, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11407v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1763, \"height\": 895, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11407v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 910, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11407v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1563, \"height\": 889, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.11407v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1787, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.11407v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 843, \"height\": 258, \"label\": \"Table\"}]"
motivation: 现有模拟器需给定初始状态，导致场景受限于初始条件且不可解释，轨迹生成也需同时学习目的地和路径。
method: 提出TrafficDiffuser，联合建模初始与目标状态的扩散模型，生成高语义场景，再将轨迹生成视为条件填充任务。
result: "在Argoverse 2上，TrafficDiffuser在目标生成和初始化上均超越基线，速度分布距离减少55.3%，离路率降低2.8%。"
conclusion: 通过可解释的高层场景生成，TrafficDiffuser简化了轨迹建模，提升了模拟器的真实性和实用性。
---

## 摘要
稳健的交通模拟器对于开发和测试自动驾驶汽车至关重要，以减少昂贵且劳动密集型的真实世界数据收集过程以及对道路物理存在的需求。然而，现有的模拟器需要智能体的初始状态来生成轨迹，由于对给定初始状态的限制，这限制了可扩展性和多样性。虽然数据驱动的智能体初始化已被广泛研究，但生成的初始状态在智能体为何在这些特定位置初始化方面不具有可解释性。在给定初始状态的情况下，轨迹生成也是一个具有挑战性的问题，因为模型必须学习目的地的不确定性以及智能体应如何随时间到达目的地。在本文中，我们提出了TrafficDiffuser，一种自上而下的交通场景生成框架，通过联合建模由初始和目标状态对定义的高层交通场景来生成它们。高层场景生成使初始状态更具可解释性，并将轨迹生成简化为简单的填充问题。我们展示了如何使用生成的高层交通场景，包括基于不同轨迹模式进行约束，以及将其与现有轨迹生成模型集成。我们在Argoverse 2运动预测数据集上进行了大量实验，以评估生成输出对真实世界分布的捕获程度。除生成目标状态外，TrafficDiffuser在智能体初始化方面优于次优方法，将速度分布距离降低了55.3%，越野率降低了2.8%。

## Abstract
Robust traffic simulators are crucial for developing and testing autonomous vehicles to reduce the costly, labor-intensive real-world data collection process and the need for physical presence on the road. However, existing simulators require agents' initial states to generate trajectories, which limits scalability and diversity due to restrictions on the given initial states. While data-driven agent initialization has been widely studied, the generated initial states are not interpretable in terms of why the agents are initialized at those specific locations. Given known initial states, trajectory generation is also a challenging problem, as the model must learn the variability of the destination and how agents should reach it over time. In this paper, we propose TrafficDiffuser, a top-down traffic scenario generation framework that generates high-level traffic scenarios, defined by initial and goal state pairs, by jointly modeling them. The high-level scenario generation makes initial states better interpretable and reduces trajectory generation into as simple as an infilling problem. We demonstrate how the generated high-level traffic scenarios can be used, including constraining based on different trajectory modes and integrating them with existing trajectory generation models. We conduct extensive experiments on the Argoverse 2 motion prediction dataset to evaluate how well the generated outputs capture real-world distributions. In addition to generating goal states, TrafficDiffuser outperforms the next-best approach for agent initialization, reducing speed distribution distance by 55.3% and the off-road rate by 2.8%.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有交通模拟器大多依赖给定的智能体初始状态来生成后续轨迹，导致两个主要瓶颈：
  - **初始状态受限**：数据驱动的初始化方法虽然能生成合理位置，但缺乏可解释性——无法解释“为什么智能体出现在这里”。
  - **轨迹生成困难**：从已知初始状态生成轨迹时，模型必须同时学习目的地的多样性以及到达目的地的动态过程，任务复杂。
- **整体含义**：论文提出一种“自上而下”的场景生成范式：先联合生成“初始状态 + 目标状态”的高层交通场景，再将轨迹生成简化为目标条件填充问题，从而提升可解释性、可行性和模块化能力。该工作服务于自动驾驶仿真测试，旨在减少真实数据采集成本和道路测试需求。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将交通场景生成分解为两个子问题：
  - **高层场景生成**：联合建模所有智能体的初始状态 \( s_0 \) 与目标状态 \( s_H \)，得到初始-目标对 `sHL = [s0, sH]`，保证两点间的可达性和社交合理性。
  - **轨迹填充**：给定高层场景和地图，预测从初始到目标之间的完整轨迹，等价于目标条件轨迹预测。
- **关键技术细节**：
  - 基于 DDPM（去噪扩散概率模型）生成高层场景。
  - **地图编码器**：采用并修改 QCNet 的地图编码器，使用异质消息传递层（HMP）处理点-多边形注意力和多边形-多边形注意力，并引入“地图摘要”（Map Summary）学习全局空间上下文。
  - **TrafficGenerator**：使用差分 Transformer（Differential Transformer）促进稀疏地图注意力；按“初始 + 目标”两帧序列编码，分别加入时间与智能体身份嵌入；注意力顺序为：地图摘要 → 时间自注意力 → 社交交叉注意力 → 局部地图注意力。
  - **引导采样**：通过梯度引导（guidance）偏置去噪过程，以增强碰撞避免和道路合规性，公式为：
    \[
    \hat{\epsilon}_\theta(s^t_{HL}, M) = \epsilon_\theta(s^t_{HL}, M) - \lambda_{guide} \nabla_{s^t_{HL}} J(\hat{s}^0_{HL})
    \]
  - **训练损失**：噪声预测损失 \( L_{HL} \) 加上注意力熵正则化 \( L_{ent} \)，鼓励稀疏地图注意力：
    \[
    L(\theta) = L_{HL}(\theta) + \lambda_{ent}L_{ent}(\theta)
    \]
  - **轨迹填充模型**：
    - 在初始和目标状态之间构建线性“桥”（Bridge）\( B \)，模型预测轨迹与桥之间的偏差。
    - 引入模式权重 \( \gamma \)，支持四种硬约束（HC）设置：同时约束初始和目标、仅约束初始、仅约束目标、无约束。
    - 轨迹重建为 \( \hat{\tau} = B + \gamma f_\phi(M, B, m) \)。
    - 损失包含重建误差 \( L_{recon} \) 和时序平滑正则 \( L_{smooth} \)。
- **算法流程（Inpainting）**：
  1. 输入地图、高层场景、时间范围和采样模式。
  2. 构建时间线性向量 \( h \)，生成桥 \( B \)。
  3. 根据模式计算权重 \( \gamma \)。
  4. 使用网络预测偏差，加权后叠加到桥上得到最终轨迹。

### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：Argoverse 2 Motion Forecasting Dataset（真实大规模自动驾驶数据集），包含 5 类智能体：车辆、公交、行人、自行车、摩托车。
- **Benchmark**：
  - 高层场景生成：常用指标 `Common Sense`（碰撞率、越野率、近边缘距离）和分布级 Jensen-Shannon Divergence（JSD），比较速度分布、横向偏差、局部密度、邻近距离等。
  - 轨迹填充：使用标准运动预测指标 ADE（平均位移误差）、FDE（最终位移误差）、MR（失误率）。
- **对比方法**：
  - 高层场景生成：对比 `SceneControl (SC)` [4] 和 `PathDiffuser` 的初始化部分（PD-init）[16]。
  - 轨迹填充：对比自身四种模式（HCboth / HCinit / HCgoal / HCnone），并展示与 `PathDiffuser` 轨迹生成模块（PD-Traj）的集成效果。

### 4. 资源与算力

- **论文未说明具体算力信息**：没有提及 GPU 型号、数量、训练时长、参数量等训练细节。文中仅说明使用 Argoverse 2 数据集，并对输入做了 z-score 归一化，但未给出硬件资源描述。

### 5. 实验数量与充分性

- **实验数量**：
  - 高层场景生成：一张定量对比表（表 I），包含初始状态和生成的目标状态两类结果，对比两个基线（SC 和 PD-init）。
  - 轨迹填充：一张定量对比表（表 II），评估了四种约束模式，并与 PD-init 轨迹生成对比。
  - 定性实验：多组可视化——高层场景多样性（图 5）、轨迹填充效果（图 6）、端到端流程及与 PD-Traj 的兼容性（图 7）。
- **充分性评估**：
  - **优点**：覆盖了高层生成、轨迹填充和端到端集成三个层面；同时使用定量和定性指标；对比了当前相关方法。
  - **不足**：
    - **缺少消融实验**：未单独验证地图摘要、差分 Transformer、注意力熵损失、不同引导函数等设计组件的贡献。
    - **缺少跨数据集验证**：仅在 Argoverse 2 单一数据集上评估，泛化性未知。
    - **未报告计算开销**：缺少推理时间、内存占用等实用性度量。
    - **轨迹填充部分没有与其他轨迹生成/预测模型做横向比较**（仅与 PD-init 对比，且 PD-init 是从高层生成后的下一步比较，而非相同输入条件下的直接对比）。

### 6. 论文的主要结论与发现

- 联合建模初始-目标状态能生成“可达”的高层交通场景，初始点与目标点之间存在符合地图结构的可行路径。
- 高层场景生成使初始状态更具可解释性——可看出车辆是静止还是行驶、意图方向等。
- 与最优基线（PD-init）相比，TrafficDiffuser 在初始化上**越野率降低 2.8%**，速度分布 JSD 距离比 SceneControl 降低 **55.3%**。
- 基于“桥 + 偏差”的轨迹填充方法在硬约束下可实现 ADE ≈ 0.52m，FDE = 0，MR = 0，接近地面真值。
- 高层场景生成器可与现有轨迹生成模型（如 PD-Traj）无缝集成，具备模块化兼容性。
- 轨迹填充中约束目标状态时 FDE 严格为零，验证了硬约束机制的有效性。

### 7. 优点：方法或实验设计上的亮点

- **可解释性强**：将初始化从“点位置”升级为“初始-目标对”，明确展示智能体意图，便于理解和调试场景。
- **任务分解优雅**：高层场景生成 + 轨迹填充，将原本复杂的轨迹生成简化为条件预测，降低建模难度。
- **模块化与兼容性**：高层生成器独立于轨迹生成器，可以替换或对接现有模型（如 PD-Traj），利于复用。
- **设计合理**：采用差分 Transformer 促进稀疏地图注意力，并引入注意力熵正则，减少了计算冗余；桥接式填充方法简单且高效。
- **评估全面**：同时考虑真实感（碰撞、越野）、分布距离（JSD）和轨迹误差（ADE/FDE/MR），并展示多样性与端到端结果。

### 8. 不足与局限

- **轨迹多样性缺失**：轨迹填充是确定性函数，给定高层场景后轨迹唯一，无法刻画多模态行为；论文也承认这是主要局限，并计划引入生成式填充。
- **高层场景生成质量仍有提升空间**：碰撞率明显高于基线（如初始状态 9.8% vs. SC 4.4%），近边缘距离也略有偏差；作者将其归因于使用多边形编码而非点编码，但未给出改进方案。
- **实验范围有限**：未做消融、未跨数据集验证、未对比更多最新方法，结论的普适性存疑。
- **指标解读需谨慎**：轨迹填充 ADE 接近 0.5m 且 FDE/MR 为零，很大程度上是因为使用了真值初始和终点作为锚点，并不能反映自由生成条件下的性能。
- **训练细节缺失**：没有提供计算资源、训练时间、超参数设置等，复现成本不透明。
- **无真实场景闭环验证**：未在模拟器中进行闭环驾驶测试，仅对静态轨迹和状态分布进行了评估，无法验证生成场景对自动驾驶策略训练的实际效用。

（完）
