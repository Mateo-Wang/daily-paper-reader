---
title: "Temporal GRPO: Beyond Trajectory-Level Credit in Vision-Language-Action Reinforcement Learning"
title_zh: 时序GRPO：超越视觉-语言-动作强化学习中的轨迹级信用分配
authors: "Yao Zhou, Hang Gao, Fengge Wu, Changwen Zheng, Wenwen Qiang"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13026v1"
tags: ["query:robotics"]
score: 9.0
evidence: 通过分阶段GRPO改进VLA强化学习中的时间信用分配
tldr: 针对基于GRPO的视觉-语言-动作模型后训练中轨迹级优势导致早期有效动作被后期失败错误惩罚的问题，提出Temporal GRPO。该方法构建可检测任务阶段，对齐阶段特定动作区间，仅比较进入相同阶段的rollout并分配阶段级优势。在RoboTwin 2.0上显著提升任务成功率和样本效率，跨任务长度一致；在LIBERO-Long上验证了分阶段更新的有效性，集中改进于结果分歧的第一阶段。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13026v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1754, \"height\": 867, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13026v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13026v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1830, \"height\": 547, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13026v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1850, \"height\": 433, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13026v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 716, \"height\": 347, \"label\": \"Table\"}]"
motivation: 轨迹级信用分配使有效早期动作受整体失败惩罚，导致信用混淆，影响VLA后训练效果。
method: 构造可检测任务阶段，对齐动作区间，仅比较同阶段rollout，计算阶段优势并应用于对应区间更新。
result: RoboTwin 2.0上提升成功率与样本效率，跨任务长度稳定；LIBERO-Long保留共享阶段并聚焦分歧点改进。
conclusion: Temporal GRPO通过阶段级信用分配有效解决轨迹级信用混淆，提升VLA强化学习性能。
---

## 摘要
结果驱动的强化学习提供了一种可扩展的方式，可以通过稀疏的任务成功反馈对视觉-语言-动作（VLA）策略进行后训练。在常见的基于GRPO的VLA后训练中，一个采样轨迹级的优势被应用于轨迹中的每个动作。因此，一个完成了多个有效阶段但随后失败的采样轨迹会惩罚产生早期进展的动作。我们将其称为轨迹级信用混叠。时序GRPO通过构建可检测的任务阶段、将每个采样轨迹与特定阶段的动作间隔对齐，并且仅比较已进入相同阶段的采样轨迹来解决这一问题。得到的阶段优势在单次策略更新中应用于其对应的间隔。在RoboTwin 2.0上，时序GRPO提高了任务成功率和样本效率，并在不同任务范围中均取得一致的收益。在LIBERO-Long上的受控更新保留了共享的先决阶段，并将改进集中在采样轨迹结果出现分歧的第一阶段。

## Abstract
Outcome-driven reinforcement learning offers a scalable way to post-train vision-language-action (VLA) policies from sparse task-success feedback. In common GRPO-based VLA post-training, one rollout-level advantage is applied to every action in the trajectory. A rollout that completes several valid stages but fails later can therefore penalize the actions that produced its earlier progress. We call this trajectory-level credit aliasing. Temporal GRPO addresses this problem by constructing detectable task stages, aligning each rollout with stage-specific action intervals, and comparing only rollouts that have entered the same stage. The resulting stage advantages are applied to their corresponding intervals in a single policy update. On RoboTwin 2.0, Temporal GRPO improves task success and sample efficiency, with consistent gains across task horizons. Controlled updates on LIBERO-Long preserve shared prerequisite stages and concentrate improvement at the first stage where rollout outcomes diverge.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- 论文聚焦于**视觉-语言-动作（VLA）模型的强化学习后训练**。结果驱动的强化学习通过稀疏的成功/失败反馈优化策略，但常用方法（如 GRPO）将**一个轨迹级优势**均匀分配给该轨迹中的**每一个动作**。
- 对于**长时程操作任务**（通常包含多个顺序阶段，如接近、抓取、搬运、放置），一个 rollout 即使顺利完成多个前置阶段，仅因最终阶段失败，其前期有效动作也会被同一个负优势惩罚。
- 论文将这一现象定义为**轨迹级信用混叠（trajectory-level credit aliasing）**：具有不同阶段进展模式的 rollout 被映射到相同的轨迹级优势，导致策略更新与局部任务进展失配，损害样本效率和子技能稳定性。
- 论文据此提出 **Temporal GRPO**，通过**可检测的任务阶段**实现时间粒度的信用分配，将阶段级优势仅分配给对应产生该阶段结果的动作区间，从而保留早期有效行为并将更新集中在结果真正分歧的阶段。

## 2. 方法论

### 核心思想

- 将任务拆解为**可检测的有序阶段序列**，将每个 rollout 与阶段对应的动作区间对齐，**仅比较进入同一阶段的 rollout**，计算阶段级组相对优势，并只将该优势应用于产生该阶段结果的动作区间。

### 关键技术细节

1. **任务条件阶段生成（Task-Conditioned Stage Generation）**
   - 使用冻结的 RynnBrain-4B 模型根据任务指令和初始视觉观测生成候选语义阶段。
   - 通过 **Stage Compiler** 将语义阶段编译为有序、具有先决依赖且可检测的信用阶段，阶段满足线性前置结构：m1 → m2 → … → mK，且最终阶段与任务成功条件一致。

2. **Rollout-to-Stage 对齐**
   - 关系检测器评估轨迹上的阶段完成条件，记录阶段 mk 首次稳定完成的时间 Ti,k。
   - 成功阶段的动作区间定义为 Bi,k = (Ti,k−1, Ti,k]；若 rollout 进入阶段但未完成，则剩余动作后缀归入该失败阶段。
   - 阶段检测仅在后训练期间使用特权仿真器状态，评估时不使用。

3. **阶段条件优势重建与策略优化**
   - 定义阶段参与变量 Vi,k（仅完成前序阶段的 rollout 计入）和阶段二元结果 Ri,k。
   - 阶段优势公式为：

     μk = Σ Vi,k Ri,k / Σ Vi,k，σk = √(Σ Vi,k (Ri,k − μk)² / Σ Vi,k)

     Âi,k = (Ri,k − μk)/(σk + ε)

   - 每个动作继承其所在阶段区间的优势：Âi,t = Σ I[t ∈ Bi,k]·Âi,k。
   - 策略更新目标与 GRPO 一致，仅将轨迹级优势替换为阶段条件时序优势，保留完整 rollout 采样、概率比和裁剪机制，使用统一目标更新单一 VLA 策略。

## 3. 实验设计

### 基准与场景

- **RoboTwin 2.0**：任务级性能与样本效率评估，涵盖短时程、中时程、长时程和超长时程任务。
- **LIBERO-Long**：受控的信用分配分析和消融实验（任务具有清晰阶段依赖和共享前置阶段）。
- 四个标准 LIBERO 数据套件的完整结果在补充材料中。

### 对比方法

- VLA 基线：π0、RDT-1B、OpenVLA-OFT（SFT 检查点）。
- VLA-RL 方法：SimpleVLA-RL、TGRPO、Trajectory-GRPO（轨迹级优势）、Stage-Reward GRPO（阶段奖励 + 轨迹级优势分配）、Temporal GRPO（本文方法）。
- 所有受控 RL 方法共享相同的 SFT 初始化、训练场景、rollout 预算、环境交互预算、策略更新预算和评估协议。

## 4. 资源与算力

- **论文未明确说明**使用的 GPU 型号、数量、训练时长等具体算力信息。
- 仅提及训练过程中所有方法使用**相同的 rollout/训练步数预算**（图 2 横轴显示 RL 训练步数最多为 180），所有方法在同一训练预算下对比，但未披露单个实验所需的硬件资源和时间成本。

## 5. 实验数量与充分性

### 实验数量

- **RoboTwin 2.0 主实验**（表 1）：在四个任务地平线组别上对比 7 种方法，每种 RL 方法使用 3 个随机种子报告均值±标准差。
- **样本效率分析**（图 2）：全部任务和长时程子集上的训练曲线对比。
- **受控信用分配分析**（图 3）：在 LIBERO-Long 上按第一分歧阶段对齐，报告各阶段条件的完成概率变化。
- **消融研究**（表 2）：Temporal GRPO、去除 Stage Compiler、Stage-Reward GRPO、去除 entered-stage gating、去除 same-stage grouping、Trajectory-GRPO 共 6 个变体。
- 互补材料中提供完整 LIBERO 实验、任务阶段细节、关系谓词和训练超参数。

### 充分性与公平性

- 实验设计总体上**受控良好**：受控 RL 方法从同一 SFT 检查点初始化、使用同一训练预算，有助于隔离信用分配规则的影响。
- 消融实验系统性地验证了各组件（阶段编译、进入阶段门控、同阶段分组）的贡献。
- 局限性在于种子数量（3 个）较少，且未报告置信区间宽度；算力资源未披露可能影响可复现性判断。

## 6. 主要结论与发现

- Tempooral GRPO 在 RoboTwin 2.0 上达到 **75.8% 的宏平均成功率**，比最强受控基线（SimpleVLA-RL，68.8%）高出 **7.0 个百分点**，并在所有任务地平线组别中取得 6.2–8.3 个百分点的**一致增益**。
- 在样本效率方面，Temporal GRPO 在相同环境交互预算下**始终优于 Trajectory-GRPO**，且长时程任务上的优势更显著。
- LIBERO-Long 受控分析表明：Trajectory-GRPO 会**负向影响共享前置阶段**（同化已经成功的动作），而 Temporal GRPO 将前置阶段的变化**保持在接近零**，并将最大正向改进集中在**结果首次分歧的阶段**。
- 消融实验确认：去除同阶段分组影响最大（99.1→90.6），证明跨越不同阶段的动作区间无法构成可靠的相对比较组；去除进入阶段门控也带来明显退化（→92.5）。

## 7. 优点

- **针对性强**：精准界定并形式化了轨迹级信用混叠问题，问题明确、动机清晰。
- **保持 VLA 架构不变**：无需引入价值模型或修改策略结构，工程成本低。
- **统一优化机制**：阶段区间在同一个训练批次和统一目标中联合优化，不涉及多策略独立训练。
- **时间粒度细化**：每个动作继承至多一个阶段优势，区间有序且不重叠，保证信用分配的一致性；对齐方式适应不同 rollouts 的时长差异，而非固定时间步。
- **与整体目标一致**：最终阶段与任务成功条件一致，不因局部奖励而偏离原始任务目标。
- **受控实验设计良好**：图 3 的阶段条件概率变化直接验证了信用分配机制，消融实验有说服力。

## 8. 不足与局限

- **依赖可靠的阶段谓词**：阶段检测需要明确的完成条件和稳定判定，关系检测器依赖特权仿真器状态，真实物理场景中难以直接应用。
- **阶段结构受限**：仅支持**线性、有先决依赖、无重复的阶段顺序**，无法处理阶段边界模糊、分支结构、循环或恢复退化（如任务中需要反复执行同一阶段）等复杂情况。
- **评估覆盖有限**：主实验仅在 RoboTwin 2.0 和 LIBERO 套件（仿真环境）上验证，未涉及真实机器人实验。
- **多智能体/双手协作的适用性**：阶段划定可能在高动态或双手协调场景中面临对齐困难。
- **算力与种子数**：未披露具体训练资源，3 个种子的统计效力有限。
- **框架扩展方向**：论文指出去除稳定阶段检测和动态阶段图是未来工作，表明当前方法在复杂任务上的泛化仍有待加强。

（完）
